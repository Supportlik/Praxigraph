name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build the distributions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - name: Run the test suite before releasing anything
        run: |
          uv sync --locked --dev
          uv run --no-sync pytest -q

      - name: Build
        run: uv build

      - name: Check the distribution metadata
        run: uvx twine check --strict dist/*

      # A tag must name the version that is actually in the package, otherwise
      # v1.0.1 could ship 1.0.0 to PyPI, where it can never be replaced.
      - name: Verify the tag matches the package version
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          tag="${GITHUB_REF_NAME#v}"
          version="$(ls dist/*.whl | sed -E 's/.*praxigraph-([^-]+)-py3.*/\1/')"
          echo "tag=$tag built=$version"
          test "$tag" = "$version"

      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    # Trusted publishing: PyPI verifies this workflow's OIDC identity, so the
    # repository needs no API token. Configure the publisher on PyPI first.
    environment:
      name: pypi
      url: https://pypi.org/p/praxigraph
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Publish
        uses: pypa/gh-action-pypi-publish@v1.14.2

  github-release:
    name: Create the GitHub release
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          generate_release_notes: true
